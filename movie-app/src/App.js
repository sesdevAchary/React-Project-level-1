import './App.css' ; // importing the css app//
import {usestate} from 'react'// to manage states in functional component
import ReactPlayer from 'react-player'; // component for playing video around different source

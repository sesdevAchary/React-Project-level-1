1-> Pip is a package manager in python ( pip install module_name ) & .py is the extension 


def __init__(self):
        self.expense = [] - constructor function
2-> ............python first.py

3-> nameO=''.join(reversed(name)) {for string reversal}
4-> nameO= name[1:5] # starting from 1 to 4 excluding 5
5-> print(name.endswith("a")) {gives True or False}
6-> List is collection of everything but mutable but a string  and a tupple are  unchangeable 

7-> DIctionary can be chanagable

8->  if and else sentence contains a colon at the last 

9-> By default, print() adds a newline (\n) at the end of whatever it prints. end = " " bloks it
    hello 
    world    //   hello world 

    10-> Return value is given in a function to hold the value for a variable 

11-> remove(array,"word") will remove a word

12-> import random (if want to pick random choice from array-->print(random.choice(quotes)))

13->now = datetime.datetime.now()
    print("📅 Current Date and Time:", now.strftime("%d-%m-%y:%H-%M-%S"))


14->except Exception as e:
Exception is the base class for most errors in Python (e.g., FileNotFoundError, IOError, etc.).
as e assigns the actual error object to the variable e. Then we print the e ...


aare you okay ?
you got it you do it
